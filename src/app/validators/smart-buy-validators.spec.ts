import { SmartBuyValidators } from './smart-buy-validators';

describe('SmartBuyValidators', () => {
  it('should create an instance', () => {
    expect(new SmartBuyValidators()).toBeTruthy();
  });
});
